// const opps = 'ngnExt'
// console.log(opps);

setTimeout(() => {
    console.log('vscode is pretty intelligent');
    clearInterval(int)
}, 3000); // runs code IN 3 seconds, clears the interval int below end.
// basically, in the 3rd second, he runs
const int = setInterval(() => {
    console.log('put a d in her ribcage');
}, 1000); // runs code EVERY second.

console.log(__dirname);
console.log(__filename);